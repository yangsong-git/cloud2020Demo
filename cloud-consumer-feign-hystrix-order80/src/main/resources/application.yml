server:
  port: 8000

spring:
  application:
    name: order-feign-hystrix-server #服务名称

eureka:
  client:
    # eureka注册中心地址配置
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/    #集群版

feign:
  hystrix:
    enabled: true